# Lecture 15: Making our helicopters hover

Today we got more practice using our ODE solver class. We used it to develop a model for the pendulums that we captured data from in class, and learned a little bit about feedback control.

In today's class we did two things:
- Developed a simulator for a pendulum calibrated to our sensor data
- Used that model to design a *proportional control* policy to make our helicopter hover at a desired angle.

There was no assignment collected because the amazon web servers have crashed around the country. Instead, you will use the code from today to complete HW4 (Due 10/29).